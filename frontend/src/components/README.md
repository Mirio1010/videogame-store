# Components Folder

This folder contains reusable React components. Components are small, self-contained pieces of UI (like buttons, headers, forms) that can be used throughout the app.

- Each file typically exports a single component.
- Components should be focused on a single responsibility.

Example: `Header.jsx` is a header component used at the top of the app.

Add new components here as your UI grows.
